import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Barbearia-Vicente-Angular',
  webDir: 'www'
};

export default config;
